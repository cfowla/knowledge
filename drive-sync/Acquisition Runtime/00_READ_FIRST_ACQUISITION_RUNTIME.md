# Acquisition Runtime — active path

The active scholarly acquisition implementation is:

`Acquisition Runtime / scholar-acquire-chatgpt / 00 - CURRENT AUTHORITATIVE RUNTIME / v0.4.3 - CURRENT AUTHORITATIVE BUILD`

Do not execute or promote material from `90 - ORIGINAL PRE-FORK (HISTORICAL)`.

Inside `scholar-acquire-chatgpt`, resolve current authority from `CURRENT_RUNTIME_POINTER.json`. The pointer's Drive IDs and SHA-256 values control over folder names, modified dates, search ordering, or prompt numbers.
